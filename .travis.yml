language: python
python:
  - "2.7"
# command to install dependencies
install: "pip install -r requirements.txt"
# command to run tests
script: nosetests --nocapture -v -w tests
notifications:
  email:
    - sathley@appacitive.com
    - singhalvibhor05@gmail.com
